@model RolePermissionViewModel
@if (Model.Permissions.Any())
{
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Module</th>
                <th>Sub Module</th>
                <th>View</th>
                <th>Add</th>
                <th>Edit</th>
                <th>Delete</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < Model.Permissions.Count; i++)
            {
                <tr>
                    @Html.HiddenFor(m => m.Permissions[i].fkIntRoleId)
                    @Html.HiddenFor(m => m.Permissions[i].PermissionId)
                    <td>@Html.TextBoxFor(m => m.Permissions[i].Module, new { @class = "form-control",@readonly="readonly" })</td>
                    <td>@Html.TextBoxFor(m => m.Permissions[i].SubModule, new { @class = "form-control", @readonly = "readonly" })</td>
                    <td>@Html.CheckBoxFor(m => m.Permissions[i].BitView, new { @class = "form-check form-check-input" })</td>
                    <td>@Html.CheckBoxFor(m => m.Permissions[i].BitAdd, new { @class = "form-check form-check-input" })</td>
                    <td>@Html.CheckBoxFor(m => m.Permissions[i].BitEdit, new { @class = "form-check form-check-input" })</td>
                    <td>@Html.CheckBoxFor(m => m.Permissions[i].BitDelete, new { @class = "form-check form-check-input" })</td>
                    <td>@Html.CheckBoxFor(m => m.Permissions[i].BitStatus, new { @class = "form-check form-check-input" })</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div class="alert alert-info">No permissions assigned. Please add new permissions.</div>
}